import Logo from "../../Logo";
import MenuList from "../molecules/NavMenuList";

const NavBar = () => {
  return (
    <div>
      <Logo />
      <MenuList />
    </div>
  );
};

export default NavBar;
