import styled from "styled-components";

function UserImg() {
  return <Img />;
}

export default UserImg;

const Img = styled.div`
  width: 4rem;
  height: 4rem;
  border-radius: 50%;
  background: white;
  cursor: pointer;
`;
